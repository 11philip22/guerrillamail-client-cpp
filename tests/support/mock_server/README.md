# Mock Server Placeholder

This directory is reserved for future local mock-server assets used by integration tests.

`WOL-166` intentionally creates only the location and shared test-support shape. A later ticket should add the actual mock-server implementation and fixtures.
